import Logo from "../assets/imgs/logo.svg";

const Login = () => {
  return (
      <div>
        <h1>어쩌고</h1>

        <img src={Logo} alt="logo" className="my-4h-[35px] flex justify-center" />
        {/* 이메일 / 비밀번호 */}

        {/* 로그인 버튼 */}

        {/* 회원가입하러 가기 */}
      </div>
  );
};

export default Login;
